---
# ── kubectl ──────────────────────────────────────────────────
- name: Get latest kubectl version
  uri:
    url: https://dl.k8s.io/release/stable.txt
    return_content: yes
  register: kubectl_version

- name: Download kubectl
  get_url:
    url: "https://dl.k8s.io/release/{{ kubectl_version.content | trim }}/bin/linux/amd64/kubectl"
    dest: /tmp/kubectl
    mode: "0755"

- name: Install kubectl
  copy:
    src: /tmp/kubectl
    dest: /usr/local/bin/kubectl
    mode: "0755"
    remote_src: yes
  become: yes

- name: Verify kubectl
  command: kubectl version --client
  register: kubectl_ver
  changed_when: false

- name: Print kubectl version
  debug:
    msg: "{{ kubectl_ver.stdout }}"

# ── Helm ─────────────────────────────────────────────────────
- name: Install Helm
  shell: |
    curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
  args:
    creates: /usr/local/bin/helm
  become: yes

- name: Verify Helm
  command: helm version
  register: helm_ver
  changed_when: false

- name: Print Helm version
  debug:
    msg: "{{ helm_ver.stdout }}"

# ── .kube directories ─────────────────────────────────────────
- name: Create .kube directory for ubuntu user
  file:
    path: /home/ubuntu/.kube
    state: directory
    owner: ubuntu
    group: ubuntu
    mode: "0700"
  become: yes

- name: Create .kube directory for jenkins user
  file:
    path: /var/lib/jenkins/.kube
    state: directory
    owner: jenkins
    group: jenkins
    mode: "0700"
  become: yes

# ── EKS kubeconfig for ubuntu user ───────────────────────────
- name: Configure kubectl for EKS (ubuntu user)
  shell: |
    aws eks update-kubeconfig \
      --region {{ eks_region }} \
      --name {{ eks_cluster_name }}
  become: yes
  become_user: ubuntu
  environment:
    HOME: /home/ubuntu
    AWS_DEFAULT_REGION: "{{ eks_region }}"

- name: Set correct ownership on ubuntu kubeconfig
  file:
    path: /home/ubuntu/.kube/config
    owner: ubuntu
    group: ubuntu
    mode: "0600"
  become: yes

- name: Verify EKS connection as ubuntu user
  command: kubectl get nodes
  become: yes
  become_user: ubuntu
  environment:
    HOME: /home/ubuntu
    KUBECONFIG: /home/ubuntu/.kube/config
  register: eks_nodes_ubuntu
  changed_when: false

- name: Print EKS nodes
  debug:
    msg: "{{ eks_nodes_ubuntu.stdout_lines }}"

# ── Copy kubeconfig to Jenkins user ──────────────────────────
# Jenkins pipeline runs kubectl commands
# Jenkins user needs kubeconfig to authenticate with EKS

- name: Copy kubeconfig to Jenkins user
  copy:
    src: /home/ubuntu/.kube/config
    dest: /var/lib/jenkins/.kube/config
    remote_src: yes
    owner: jenkins
    group: jenkins
    mode: "0600"
  become: yes

- name: Verify Jenkins can reach EKS
  command: kubectl get nodes
  become: yes
  become_user: jenkins
  environment:
    HOME: /var/lib/jenkins
    KUBECONFIG: /var/lib/jenkins/.kube/config
  register: eks_nodes_jenkins
  changed_when: false

- name: Print EKS nodes as Jenkins user
  debug:
    msg: "{{ eks_nodes_jenkins.stdout_lines }}"

- name: Confirm Jenkins kubectl access
  debug:
    msg: |
      Jenkins user can reach EKS successfully
      Nodes: {{ eks_nodes_jenkins.stdout_lines | length }} node(s) found

# NGINX Ingress Controller
- name: Add ingress-nginx Helm repository
  shell: |
    helm repo add ingress-nginx \
      https://kubernetes.github.io/ingress-nginx
    helm repo update
  become_user: ubuntu
  environment:
    HOME: /home/ubuntu
    KUBECONFIG: /home/ubuntu/.kube/config
  changed_when: false

- name: Check if ingress-nginx already installed
  shell: |
    helm list -n ingress-nginx --short | grep -c ingress-nginx || true
  become_user: ubuntu
  environment:
    HOME: /home/ubuntu
    KUBECONFIG: /home/ubuntu/.kube/config
  register: ingress_check
  changed_when: false
  failed_when: false

- name: Install NGINX Ingress Controller with NLB
  shell: |
    helm install ingress-nginx ingress-nginx/ingress-nginx \
      --namespace ingress-nginx \
      --create-namespace \
      --set controller.replicaCount=2 \
      --set controller.service.type=LoadBalancer \
      --set controller.service.annotations."service\.beta\.kubernetes\.io/aws-load-balancer-type"=nlb \
      --set controller.service.annotations."service\.beta\.kubernetes\.io/aws-load-balancer-backend-protocol"=tcp \
      --set controller.service.externalTrafficPolicy=Local \
      --wait \
      --timeout 5m
  become_user: ubuntu
  environment:
    HOME: /home/ubuntu
    KUBECONFIG: /home/ubuntu/.kube/config
  when: ingress_check.stdout == "0" or ingress_check.stdout == ""

- name: Skip if already installed
  debug:
    msg: "ingress-nginx already installed — skipping"
  when: ingress_check.stdout != "0" and ingress_check.stdout != ""

- name: Wait for ingress controller pods to be ready
  shell: |
    kubectl wait \
      --namespace ingress-nginx \
      --for=condition=ready pod \
      --selector=app.kubernetes.io/component=controller \
      --timeout=180s
  become_user: ubuntu
  environment:
    HOME: /home/ubuntu
    KUBECONFIG: /home/ubuntu/.kube/config

- name: Verify ingress controller pods
  shell: kubectl get pods -n ingress-nginx
  become_user: ubuntu
  environment:
    HOME: /home/ubuntu
    KUBECONFIG: /home/ubuntu/.kube/config
  register: ingress_pods
  changed_when: false

- name: Print ingress pods
  debug:
    msg: "{{ ingress_pods.stdout_lines }}"

- name: Wait for NLB DNS to be assigned
  shell: |
    kubectl get service ingress-nginx-controller \
      -n ingress-nginx \
      -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'
  become_user: ubuntu
  environment:
    HOME: /home/ubuntu
    KUBECONFIG: /home/ubuntu/.kube/config
  register: nlb_dns
  retries: 20
  delay: 30
  until: nlb_dns.stdout != ""
  changed_when: false

- name: Print NLB DNS — save for Traffic Manager
  debug:
    msg: |
      NLB DNS (save for Traffic Manager):
      {{ nlb_dns.stdout }}


     