# kustomization file for Kubernetes resources to be deployed on AWS EKS cluster
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yml
  - deployment.yml
  - service.yml
  - ingress.yml